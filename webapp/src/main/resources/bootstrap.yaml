spring:
  application:
    name: webapp
  cloud:
    config:
      discovery:
        enabled: false
        serviceId: config-server
security:
  user:
    password: foo
welcome:
  string: dev
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
  instance:
    hostname: localhost