spring:
  application:
    name: webapp
  thymeleaf:
    cache: false
  cloud:
    config:
      discovery:
        enabled: false
        serviceId: config-server
welcome:
  string: dev
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
  instance:
    hostname: localhost
