server:
  port: 8081
spring:
  application:
    name: service
  cloud:
    config:
      discovery:
        enabled: false
        serviceId: config-server
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
  instance:
    hostname: localhost